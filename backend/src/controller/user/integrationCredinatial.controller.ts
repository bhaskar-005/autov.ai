import { Request, Response } from "express";
import { response } from "../../utils/responseFormate";

const saveYoutubeCredentials:any = async(req:Request, res:Response)=> {
    try {
       
    } catch (error) {
      console.log(error);
      // return response() 
    }
}